generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql" // 1. sqlite에서 postgresql로 변경
  url      = env("DATABASE_URL")
}

model User {
  id           String   @id @default(uuid()) // 2. PostgreSQL 호환을 위해 uuid() 추천
  email        String   @unique
  passwordHash String
  nickname     String
  lyrics       Lyric[]
  createdAt    DateTime @default(now())
}

model Lyric {
  id         String      @id @default(uuid())
  title      String
  body       String
  imageUrl   String
  bpm        Int?
  key        String?
  songForm   String      @default("Verse / Hook")
  isPublic   Boolean     @default(false)
  folder     String      @default("Inbox")
  progress   String      @default("Draft")
  authorId   String
  author     User        @relation(fields: [authorId], references: [id], onDelete: Cascade)
  rhymeNotes RhymeNote[]
  createdAt  DateTime    @default(now())
  updatedAt  DateTime    @updatedAt
}

model RhymeNote {
  id           String   @id @default(uuid())
  lyricId      String
  lyric        Lyric    @relation(fields: [lyricId], references: [id], onDelete: Cascade)
  vowelPattern String
  memo         String
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt
}